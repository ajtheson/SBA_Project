# ========================
# SERVER
# ========================
server.port=8080

# ========================
# SQL SERVER DATASOURCE
# ========================
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=QuizOnlineSystem;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=123
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sa}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# ========================
# JPA / HIBERNATE
# ========================
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ========================
# CORS (for React frontend)
# ========================
app.cors.allowed-origins=http://localhost:5173

# ========================
# JWT
# ========================
app.jwt.secret=QuizOnlineSystemSecretKeyForJWTTokenGeneration2026SBAProject
app.jwt.expiration=86400000
# app.jwt.secret=${APP_JWT_SECRET}
# app.jwt.expiration=${APP_JWT_EXPIRATION:86400000}

# ========================
# MAIL (reuse from old project)
# ========================
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=thesondayne@gmail.com
spring.mail.password=ecgeltooxxdhhvmt
# spring.mail.username=${SPRING_MAIL_USERNAME}
# spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.protocol=smtp
